# ============================================================
# 小雅语音助手 依赖清单（按模块分组，可直接 pip install -r 全部安装）
# ============================================================

# ---- 核心通信（本轮已实现，必须安装）----
# 与 Hana server 的 HTTP + WebSocket 通信（core/hana_client.py）
websocket-client>=1.7.0

# ---- 阶段一：录音 + 转写 ----
# 麦克风采集与音频播放（main.py 录音模块 / tts 播放）
sounddevice>=0.4.6
# 音频数组运算（录音缓冲、静音判断）
numpy>=1.24
# 语音转文字，模型首次运行时自动下载（core/stt.py）
faster-whisper>=1.0.0

# ---- 阶段三：语音合成 ----
# 微软在线 TTS，合成需要联网（core/tts.py）
edge-tts>=6.1.0

# ---- 阶段四：唤醒词 ----
# 本地唤醒词检测，个人/非商用免费（core/wake.py）
pvporcupine>=3.0.0
